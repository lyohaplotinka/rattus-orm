---
sidebar_position: 1
title: "Introduction"
---
import PreviewIframe from '@site/src/components/shared/PreviewIframe'
import { getIntegrationByTitle } from '../../../src/integrations'

`@rattus-orm/pinia` is a Data Provider that connects the [Pinia](https://pinia.vuejs.org/) store with Rattus ORM.

The package includes:
1. PiniaDataProvider
2. compositions `useRattusContext`, `useRepository`
3. a Vue plugin that sets up the ORM connection with Pinia.

### Live demo
<PreviewIframe src={getIntegrationByTitle('Pinia').liveDemoUrl} />
