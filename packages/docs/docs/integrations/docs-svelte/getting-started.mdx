---
sidebar_position: 1
title: "Getting started"
---
import PreviewIframe from '@site/src/components/shared/PreviewIframe'
import { getIntegrationByTitle } from '../../../src/integrations'

`@rattus-orm/svelte` is a package that links [Svelte](https://svelte.dev/) with Rattus ORM.

The package includes:
1. hooks `useRattusContext`, `useRepository`
2. the component `<RattusProvider />`.

### Live demo
<PreviewIframe src={getIntegrationByTitle('Svelte').liveDemoUrl} />
